export type WorkPeriod = {
    id: number;
    startTime: string;
}

export type WorkPeriodAggregateSaleItem = {
    name: string;
    total: number;    
}

export type GrossSalesDataModel = {
    totalSales: string;
    details: string;
}